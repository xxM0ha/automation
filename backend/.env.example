# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL)
DB_NAME=prime_automation
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_PORT=5432

# Laffe POS — Playwright scraping (temporary until official API)
LAFFE_URL=https://pos.laffe.com
LAFFE_USERNAME=your_username
LAFFE_PASSWORD=your_password

# Polling interval in seconds
ORDER_POLL_INTERVAL=30
